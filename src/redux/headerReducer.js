const initialState = {}

// export const headerReducer = (state = initialState, action) => {
// 	switch (action.type) {
// 		case first:
// 			return { ...state, ...payload }

// 		default:
// 			return state
// 	}
// }
